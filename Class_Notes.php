<?php

function isNotZero($argument)
FINISH THIS FROM MY PHOTO
{
	if ($argument != =) {
		return true;
	} else {
		return false;
	}
}

function isValid($argument) {
	if (ifValid($a) == false) {
		showError($a);
	}
	if (isValid($b) == false) {
		showError($b);
	}
}
